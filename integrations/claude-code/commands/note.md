---
description: Quick note to global inbox
argument-hint: <note text — random thought / idea / followup>
---

```bash
__SYSTEM_DIR__/bin/vault note "$ARGUMENTS"
```

Appends timestamped line to `__AUTOFLOW_VAULT__/_system/inbox.md`. Process weekly. No project context required.

After running, confirm "added to inbox" only — don't paste full file.
